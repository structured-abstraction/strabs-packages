[project]
name = "strabs-helm"
version = "0.1.0"
description = "Type-safe interface for Helm chart operations"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=9.0", "ruff>=0.14", "mypy>=1.19"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/strabs"]
